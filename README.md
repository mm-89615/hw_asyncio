Указать необходимые переменные окружения в .env

Установить зависимости с помощью poetry:
```
poetry install
```

Запустить докер контейнер с помощью docker-compose:
```
docker-compose up --build
```

